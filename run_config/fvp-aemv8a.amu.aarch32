#!/usr/bin/env bash

post_fetch_tf_resource() {
        local model="base-aemv8a"

	model="$model" \
	arch_version="8.4" \
	amu_present="1" \
	aarch32="1" \
	cache_state_modelled="0" \
		gen_model_params

	model="$model" gen_fvp_yaml
}
